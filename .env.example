# ============================================================
# Lumina — environment configuration TEMPLATE
# Copy to .env and fill in real values:   cp .env.example .env
# NEVER commit the real .env file.
# ============================================================

# ── Application ─────────────────────────────────────────────
APP_ENV=development                # development | staging | production
APP_DEBUG=true                     # true locally, ALWAYS false in production
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
# Blank is allowed only in self-hosted mode and creates an ephemeral key.
JWT_SECRET_KEY=
# Required in hosted mode; identifies the intended bootstrap administrator.
BOOTSTRAP_ADMIN_EMAIL=
# Required in hosted mode; send it once as X-Bootstrap-Token during registration.
BOOTSTRAP_ADMIN_TOKEN=

# ── Deployment mode (drives DB / vector / storage selection) ─
# self_hosted -> SQLite + ChromaDB + local filesystem storage
# hosted      -> PostgreSQL + pgvector; local files until cloud storage is added
DEPLOYMENT_MODE=self_hosted

# ── Structured database ─────────────────────────────────────
# self_hosted example: sqlite:///./data/lumina.db
# hosted example:      postgresql+psycopg://USER:PASSWORD@HOST:5432/lumina
DATABASE_URL=sqlite:///./data/lumina.db

# ── Vector search / document storage ────────────────────────
CHROMA_PERSIST_DIRECTORY=./data/chroma
STORAGE_BACKEND=local
STORAGE_NAMESPACE=self-hosted
UPLOAD_DIRECTORY=./data/uploads
MAX_UPLOAD_SIZE_BYTES=52428800
MAX_REQUEST_SIZE_BYTES=1048576
MAX_CONCURRENT_DOCUMENT_VALIDATIONS=2
MAX_DOCUMENTS_PER_COURSE=1000
MAX_COURSE_STORAGE_BYTES=2147483648
MAX_PDF_PAGES=500
MAX_PDF_PAGE_PIXELS=40000000
MAX_PDF_TOTAL_PIXELS=100000000
MAX_PDF_CONTENT_STREAM_BYTES=5242880
MAX_PDF_DRAWING_OPERATIONS=100000

# ── AI providers ────────────────────────────────────────────
AI_PROVIDER=ollama                 # ollama | openai | gemini | claude
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1
OPENAI_API_KEY=sk-your-key-here
GEMINI_API_KEY=your-gemini-key-here
ANTHROPIC_API_KEY=sk-ant-your-key-here
