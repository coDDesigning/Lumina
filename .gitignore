# ── Secrets / environment ────────────────────────────────
# Never commit real credentials. Only the example template is allowed.
.env
.env.*
!.env.example

# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# ── Node / frontend ──────────────────────────────────────
node_modules/
dist/
build/
coverage/

# ── Databases & vector stores (self-hosted mode) ─────────
*.sqlite3
*.db
data/
chroma/
chroma_db/

# ── User-uploaded content & logs ─────────────────────────
uploads/
*.log
logs/
backups/

# ── IDE / OS noise ───────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Temp files ───────────────────────────────────────────
*.tmp
*.bak

# ── Manual deploy scratch (deploy_local.py) ──────────────
deploy_state.json

.user/

# Terraform (AWS infrastructure)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
override.tf
override.tf.json
.terraform.tfstate.lock.info

# ── Local ops scratch scripts (not part of the app) ──────
/roll.py
/roll_services.py
/status.py
/fix_worker_command.py
/set_env_and_roll.py
.claude/
.opencode/
opencode/
graphify-out/
graphiy-out/
.playwright-mcp/
network-dump*.txt
